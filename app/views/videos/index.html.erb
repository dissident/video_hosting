<h1>Listing Videos of <%= current_user.email %></h1>

<% if @videos.present?%>
  <% @videos.each do |video| %>
    <%= render 'shared/video_block', locals: { video: video } %>
  <% end %>
<% end %>


<%= link_to 'New Video', new_video_path %>
